import React from "react"
import "./styles.scss";

const Loader = () => {
    return (
        <div className="loader_wrap">
            <span className="loader"></span>
        </div>
    );
};

export default Loader;